<!DOCTYPE html>
<html>
  <head>
    <title>Lamebook</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  </head>

  <body>
    <nav class="navbar bg-light">
      <div class="container-fluid">
        <%= link_to "Lamebook", root_path, class: "navbar-brand" %>
        <div class="d-flex">
          <% if user_signed_in? %>
            <%= link_to "Sair", destroy_user_session_path, class: "btn btn-primary" %>
          <% else %>
            <%= link_to "Entrar", new_user_session_path, class: "btn btn-primary me-2" %>
            <%= link_to "Cadastre-se", new_user_registration_path, class: "btn btn-success" %>
          <% end %>
        </div>
      </div>
    </nav>
    <main>
      <div class="container">
        <% flash.each do |key, value| %>
          <div class="<%= flash_class(key) %>">
            <%= value %>
          </div>
        <% end %>
        <%= yield %>
      </div>
    </main>
  </body>
</html>
